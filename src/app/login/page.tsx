"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight,CheckCircle2,Factory,ShieldCheck } from "lucide-react";
export default function LoginPage(){const router=useRouter();const[email,setEmail]=useState("admin@demo.com");const[password,setPassword]=useState("Demo@1234");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});const j=await r.json();setLoading(false);if(!r.ok)return setError(j.error);router.push("/dashboard");router.refresh()}return <main className="login-shell"><section className="login-brand"><div className="login-logo"><Factory/><span>Sistema <strong>Industrial OS</strong></span></div><div className="login-copy"><span className="pill">ENTERPRISE PERFORMANCE OPERATING SYSTEM</span><h1>Uma empresa inteira.<br/><em>Uma única operação.</em></h1><p>ERP corporativo modular para comercial, compras, estoque, produção, finanças, RH e logística com isolamento multiempresa e rastreabilidade.</p><div className="benefits"><span><CheckCircle2/> Processos integrados</span><span><ShieldCheck/> Tenant isolado e auditável</span></div></div><small>Ambiente de desenvolvimento isolado do repositório Sistema-Industrial.</small></section><section className="login-form"><form onSubmit={submit}><span className="eyebrow-dark">ACESSO CORPORATIVO</span><h2>Entre na plataforma</h2><p>Use suas credenciais para acessar o ambiente da empresa.</p><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<button className="primary login-submit" disabled={loading}>{loading?"Entrando...":<>Entrar <ArrowRight/></>}</button><div className="dev-access"><strong>Acesso de desenvolvimento</strong><span>admin@demo.com · Demo@1234</span><small>Remova/rotacione antes de produção.</small></div></form></section></main>}
