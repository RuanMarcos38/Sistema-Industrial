import { db } from "@/lib/db";
import { requireSession } from "@/lib/auth";
import { apiError,ok } from "@/lib/api";
export async function GET(){try{const {tenantId}=await requireSession();const [customers,suppliers,products,employees,openSales,openPurchase,production,shipments,receivables,payables]=await Promise.all([db.customer.count({where:{tenantId,status:"active"}}),db.supplier.count({where:{tenantId,status:"active"}}),db.product.count({where:{tenantId,status:"active"}}),db.employee.count({where:{tenantId,status:"active"}}),db.salesOrder.aggregate({where:{tenantId,status:"open"},_sum:{totalCents:true},_count:true}),db.purchaseOrder.aggregate({where:{tenantId,status:"open"},_sum:{totalCents:true},_count:true}),db.productionOrder.count({where:{tenantId,status:{not:"completed"}}}),db.shipment.count({where:{tenantId,status:{not:"delivered"}}}),db.receivable.aggregate({where:{tenantId,status:"open"},_sum:{amountCents:true}}),db.payable.aggregate({where:{tenantId,status:"open"},_sum:{amountCents:true}})]);return ok({customers,suppliers,products,employees,openSalesCount:openSales._count,openSalesCents:openSales._sum.totalCents??0,openPurchaseCount:openPurchase._count,openPurchaseCents:openPurchase._sum.totalCents??0,production,shipments,receivableCents:receivables._sum.amountCents??0,payableCents:payables._sum.amountCents??0,updatedAt:new Date().toISOString()})}catch(error){return apiError(error)}}
